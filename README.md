## Grep-Lite
This project is designed to be able to filter through a file of text using a regex (regular expression) pattern and return a match of each line that matches the search term or pattern followed by the line index.

Built using Rust.